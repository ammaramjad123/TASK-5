class triangle{
private:

	double base;
	double height;

public:

	double ba, he;
	void init(double,double);
	void display (double,double);
	double calculatearea(double, double);
	double area;
};